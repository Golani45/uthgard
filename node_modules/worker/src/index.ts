export interface Env {
  // KV: KVNamespace;        // uncomment when you bind KV
  // DISCORD_WEBHOOK_URL: string; // example secret
}

export default {
  async fetch(_req: Request, _env: Env, _ctx: ExecutionContext): Promise<Response> {
    return new Response("worker is alive");
  },

  // cron handler (ScheduledController is the correct type name)
  async scheduled(_event: ScheduledController, _env: Env, _ctx: ExecutionContext) {
    // no-op for now
  }
};