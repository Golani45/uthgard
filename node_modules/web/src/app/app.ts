import { Component, signal } from '@angular/core';
import { RouterOutlet } from '@angular/router';

@Component({
  selector: 'app-root',
  imports: [RouterOutlet],
  templateUrl: './app.html',
  styleUrl: './app.scss',
})
export class App {
  protected readonly title = signal('web');

  //TODO:
  // Scraper to pull Herald data
  // Database to store player & keep activity
  // Frontend to display:
  // Real-time keep status
  // Player realm activity
  // Discord bot for alerts
}
