export const environment = {
  production: false,
  dataBaseUrl: "data",
  // use the production Worker URL for now so you don't need wrangler dev running
  apiBaseUrl: "https://uthgard-herald.golani45.workers.dev",
};
